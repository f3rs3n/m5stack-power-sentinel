# Grow through independently scoped modules

Power Sentinel is evolving from a clean NUT Monitor baseline into a local homelab companion, but future capabilities should enter as independently scoped modules rather than by restoring the old coupled multi-dashboard surface. Each module should have its own operational responsibility, contract, tests, and user-facing surface so the product can grow without turning every integration into a shared maintenance burden.
